@attribute [InputFieldControlAttribute(typeof(string))]

<Textbox Value="@Value" ValueChanged="(string newValue) => { Value = newValue; ValueChanged.InvokeAsync(Value); }" />

@code {
    [Parameter] public string Value { get; set; }
    [Parameter] public EventCallback<string> ValueChanged { get; set; }
}