<Project>

  <PropertyGroup>
    <Title>Aurora</Title>
    <Description>Unified Lighting Effects across different RGB devices</Description>
    <Product>Aurora</Product>
    <Copyright>Copyright ©  2017</Copyright>
    <Version>1.0.0.0</Version>

    <FileVersion Condition=" '$(FileVersion)' == '' ">$(APPVEYOR_BUILD_NUMBER)</FileVersion>
    <FileVersion Condition=" '$(FileVersion)' == '' ">$(BUILD_BUILDNUMBER)</FileVersion>
    <FileVersion Condition=" '$(FileVersion)' == '' ">0.8.0</FileVersion>

    <InformationalVersion Condition=" '$(APPVEYOR_REPO_COMMIT)' != '' ">$(Version) - Commit Hash: $(APPVEYOR_REPO_COMMIT)</InformationalVersion>
    <InformationalVersion Condition=" '$(BUILD_SOURCEVERSION)' != '' ">$(Version) - Commit Hash: $(BUILD_SOURCEVERSION)</InformationalVersion>
  </PropertyGroup>

</Project>