<Window x:Class="Aurora.Controls.Window_Prompt"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             SizeToContent="Height"
             Background="#181818"
             WindowStyle="ToolWindow" Width="306.983">
    <StackPanel>
        <TextBlock Text="{Binding Path=Description, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}, Mode=OneWay}" Margin="8" TextWrapping="Wrap" />
        <TextBlock Text="{Binding Path=ErrorMessage, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}, Mode=OneWay}" Foreground="Red" Margin="8,0,8,8" TextWrapping="Wrap" />
        <TextBox Margin="8,0" Text="{Binding Path=Text, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="8">
            <Button Content="Cancel" Padding="6,2" Margin="0,0,8,0" Click="CancelButton_Click" />
            <Button Content="Okay" Padding="6,2" Click="OkayButton_Click" />
        </StackPanel>
    </StackPanel>
</Window>
