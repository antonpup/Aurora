<Window  x:Class="Aurora.Settings.DeviceLayoutViewer.Window_DeviceConfig"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Aurora.Settings.DeviceLayoutViewer"
    Title="Device Config"
    MinHeight="100"
    Height="500"
    MinWidth="300"
    ResizeMode="CanResizeWithGrip"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterOwner">
    <Grid HorizontalAlignment="Left" VerticalAlignment="Top">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="340" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="400"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <local:Control_DeviceLayout x:Name="deviceLayout"  Grid.Column="0"/>
        <StackPanel Grid.Column="1" Margin="5" Background="Gray">

            <StackPanel Margin="5" Orientation="Horizontal">
                <StackPanel Orientation="Vertical" Width="150">
                    <TextBlock Margin="0,1,0,1" Height="22" TextWrapping="Wrap" Text="Device Type:"/>
                    <TextBlock HorizontalAlignment="Left" Margin="0,1,0,1" Height="22" TextWrapping="Wrap" Text="Device Layout:" VerticalAlignment="Top"/>
                    <TextBlock x:Name="keyboard_layout_tb" HorizontalAlignment="Left" Margin="0,1,0,1" Height="22" TextWrapping="Wrap" Text="Keyboard Layout:" VerticalAlignment="Top"/>
                </StackPanel>
                <StackPanel Orientation="Vertical">
                    <ComboBox x:Name="device_type" HorizontalAlignment="Left" Margin="0,1,0,1" VerticalAlignment="Top" Width="150" SelectionChanged="device_type_SelectionChanged"/>
                    <ComboBox x:Name="device_layout" HorizontalAlignment="Left" Margin="0,1,0,1" VerticalAlignment="Top" Width="150" SelectionChanged="device_layout_SelectionChanged"/>
                    <ComboBox x:Name="keyboard_layout" HorizontalAlignment="Left" Margin="0,1,0,1" VerticalAlignment="Top" Width="150" SelectionChanged="keyboard_layout_SelectionChanged"/>
                </StackPanel>
            </StackPanel>
            <StackPanel>
                <CheckBox x:Name="devices_disable_lighting" Content="Disable Device Lighting" Margin="0,1,0,1" Checked="device_disable_lighting_Checked" Unchecked="device_disable_lighting_Checked" />
            </StackPanel>
            <StackPanel Orientation="Vertical">
                <StackPanel Margin="5" Orientation="Horizontal">

                    <TextBlock Margin="0,1,0,1" Height="22" TextWrapping="Wrap" Text="Tag: "/>
                    <TextBox Text="{Binding Path=SelectedKey.Keycap.Config.Tag, Mode=TwoWay}" Width="30" Margin="0,1,0,1" PreviewTextInput="NumberValidationTextBox"/>
                    <TextBlock Margin="0,1,0,1" Height="22" TextWrapping="Wrap" Text="Image: "/>
                    <TextBox Text="{Binding Path=SelectedKey.Keycap.Config.Image, Mode=TwoWay}" Width="120" Margin="0,1,0,1"/>
                    <TextBlock Margin="0,1,0,1" Height="22" TextWrapping="Wrap" Text="Height: "/>
                    <TextBox Text="{Binding Path=SelectedKey.Config.Height, Mode=TwoWay}" Width="30" Margin="0,1,0,1" PreviewTextInput="NumberValidationTextBox"/>
                    <TextBlock Margin="0,1,0,1" Height="22" TextWrapping="Wrap" Text="Width: "/>
                    <TextBox Text="{Binding Path=SelectedKey.Config.Width, Mode=TwoWay}" Width="30" Margin="0,1,0,1" PreviewTextInput="NumberValidationTextBox"/>
                </StackPanel>
                <StackPanel Margin="5" Orientation="Horizontal">
                    
                    <TextBlock Margin="0,1,0,1" Height="22" TextWrapping="Wrap" Text="X: "/>
                    <TextBox  x:Name="keycap_x" Text="{Binding Path=SelectedKey.Keycap.Config.X, Mode=TwoWay}" Width="30" Margin="0,1,0,1" PreviewTextInput="NumberValidationTextBox"/>
                    <TextBlock Margin="0,1,0,1" Height="22" TextWrapping="Wrap" Text="Y: "/>
                    <TextBox  x:Name="keycap_y" Text="{Binding Path=SelectedKey.Keycap.Config.Y, Mode=TwoWay}" Width="30" Margin="0,1,0,1" PreviewTextInput="NumberValidationTextBox"/>
                    <TextBlock Margin="0,1,0,1" Height="22" TextWrapping="Wrap" Text="Height: "/>
                    <TextBox  x:Name="keycap_height" Text="{Binding Path=SelectedKey.Keycap.Config.Height, Mode=TwoWay}" Width="30" Margin="0,1,0,1" PreviewTextInput="NumberValidationTextBox"/>
                    <TextBlock Margin="0,1,0,1" Height="22" TextWrapping="Wrap" Text="Width: "/>
                    <TextBox  x:Name="keycap_width" Text="{Binding Path=SelectedKey.Keycap.Config.Width, Mode=TwoWay}" Width="30" Margin="0,1,0,1" PreviewTextInput="NumberValidationTextBox"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>
        <StackPanel  Orientation="Horizontal"  HorizontalAlignment="Right"  Grid.Row="1" Grid.Column="0">
            <Button Name="deleteButton" Click="deviceDelete_Click" IsDefault="True" Margin="5">Delete Device</Button>
            <Button Name="saveLayoutButton" Click="saveLayout_Click" IsDefault="True" Margin="5">Save Layout</Button>
        </StackPanel>
        <!-- Accept or Cancel -->
        <StackPanel Orientation="Horizontal"  HorizontalAlignment="Right"  Grid.Row="1" Grid.Column="1">
            <Button Name="okButton" Click="okButton_Click" IsDefault="True" Margin="5">OK</Button>
            <Button Name="cancelButton" Click="cancelButton_Click" IsCancel="True" Margin="5">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>