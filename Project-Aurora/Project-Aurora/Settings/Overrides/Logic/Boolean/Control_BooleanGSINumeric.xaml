<UserControl x:Class="Aurora.Settings.Overrides.Logic.Control_ConditionGSINumeric"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Aurora.Settings.Overrides.Logic"
             xmlns:controls="clr-namespace:Aurora.Controls"
             mc:Ignorable="d" d:DesignWidth="400">
    <Grid Margin="0,0,0,6">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="62px" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <controls:Control_GameStateParameterPicker x:Name="operand1Picker" SelectedPath="{Binding Operand1Path}" PropertyType="Number" Grid.Column="0" />
        <ComboBox x:Name="OperatorCb" SelectedValue="{Binding Operator}" SelectedValuePath="Value" DisplayMemberPath="Label" Grid.Column="1" Margin="6,0" />
        <controls:Control_GameStateParameterPicker x:Name="operand2Picker" SelectedPath="{Binding Operand2Path}" PropertyType="Number" Grid.Column="2" />
    </Grid>
</UserControl>
